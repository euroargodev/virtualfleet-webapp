Deployment plan 
- interpolate along line (raw lat/lon space or geodesic ?)
- shape = polygon, should be convex (every inner corner < 180 degrees), nothing fancy -> actually replaced it with a simple rectangle function

brand.yml
- typography -> check with IFREMER for that (paid license?)

Parcels
- VFleet.plot_positions() does not work anymore, Parcels change their code all the time, it often breaks stuff.

Simulation
- Being able to cancel a simulation? With a button -> does not seem trivial (async programming), can just kill the app on the terminal

- Tester sur HPC (more poweeeeer) -> need SSH tunnel for localhost app

- Prendre en comptes les 1st feedback de Kevin et Guillaume (cfr. mails)

- Remplir les TBD dans les tooltips

Don't forget
- Run ruff on repo regularly
- Put the default depth to 1 (actually seems to be 0 in the deployment plan)
- Works on my machine BUT i have a custom VirtualFleet package that is active, see my PR @https://github.com/euroargodev/VirtualFleet/pull/42
- already taken some measures for the deployment of the app on a server with multiple users, namely outsourcing "long" calculations to async threads. Keeping
that in mind, for saving the results of the simulation (for multiple users), caution must be applied and probably the session.id parameter will be useful..
